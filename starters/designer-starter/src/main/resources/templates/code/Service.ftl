package ${packageName}.service;

import io.softa.framework.orm.service.EntityService;
import ${packageName}.entity.${modelName};

/**
* ${modelName} model service interface
* @author ${userName}
* @date ${currentDate}
*/
public interface ${modelName}Service extends EntityService<${modelName}, Long> {

}